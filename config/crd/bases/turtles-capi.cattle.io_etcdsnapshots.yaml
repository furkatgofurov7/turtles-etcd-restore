---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: etcdsnapshots.turtles-capi.cattle.io
spec:
  group: turtles-capi.cattle.io
  names:
    kind: ETCDSnapshot
    listKind: ETCDSnapshotList
    plural: etcdsnapshots
    singular: etcdsnapshot
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ETCDSnapshot is the Schema for the ETCDSnapshots API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          snapshotFile:
            description: ETCDSnapshotFile defines the desired state of ETCDSnapshotFile
            properties:
              createdAt:
                description: CreatedAt is the time when the snapshot was created
                format: date-time
                type: string
              location:
                description: Location is the location of the snapshot file
                type: string
              message:
                description: Message is the message of the snapshot file
                type: string
              metadata:
                description: Metadata is the metadata of the snapshot file
                type: string
              name:
                description: Name is the name of the snapshot file
                type: string
              nodeName:
                description: NodeName is the name of the node where the snapshot was
                  taken
                type: string
              size:
                description: Size is the size of the snapshot file
                format: int64
                type: integer
              status:
                description: Status is the status of the snapshot file
                type: string
            type: object
          spec:
            description: ETCDSnapshotSpec defines the desired state of ETCDSnapshot
            properties:
              clusterName:
                type: string
            type: object
          status:
            description: ETCDSnapshotStatus defines the observed state of ETCDSnapshot
            properties:
              missing:
                description: Missing is true if the snapshot is missing
                type: boolean
            required:
            - missing
            type: object
        required:
        - status
        type: object
    served: true
    storage: true
