---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: etcdbackups.turtles-capi.cattle.io
spec:
  group: turtles-capi.cattle.io
  names:
    kind: EtcdBackup
    listKind: EtcdBackupList
    plural: etcdbackups
    singular: etcdbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EtcdBackup is the Schema for the etcdbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: backup spec
            properties:
              clusterId:
                type: string
              etcdBackupConfig:
                description: EtcdBackupConfig defines the configuration of EtcdBackup
                properties:
                  enabled:
                    type: boolean
                  intervalHours:
                    format: int64
                    type: integer
                  retention:
                    format: int64
                    type: integer
                  s3EtcdBackupConfig:
                    description: S3EtcdBackupConfig defines the configuration of S3EtcdBackup
                    properties:
                      accessKey:
                        type: string
                      bucketName:
                        type: string
                      customCa:
                        type: string
                      endpoint:
                        type: string
                      folder:
                        type: string
                      region:
                        type: string
                      secretKey:
                        type: string
                    type: object
                  safeTimestamp:
                    type: boolean
                  timeout:
                    format: int64
                    type: integer
                type: object
              filename:
                type: string
              manual:
                type: boolean
            type: object
          status:
            description: backup status
            properties:
              clusterObject:
                type: string
              conditions:
                items:
                  description: EtcdBackupCondition defines the condition of EtcdBackup
                  properties:
                    lastTransitionTime:
                      type: string
                    lastUpdateTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              kubernetesVersion:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
